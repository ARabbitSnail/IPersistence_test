<mapper namespace="product">
    <select id="selectAll">
        select * from product
    </select>

    <select id="selectOne">
        select * from product where id = ? and username = ?
    </select>
</mapper>